---
name: {{NAME}}
description: |-
  {{DESCRIPTION}}
---

# {{NAME}} — orchestration playbook

> Loaded as an Antigravity CLI (agy) skill. Active in any agy session
> started in a repo where the `crew` MCP server is registered
> (`.agents/mcp_config.json`). Launch agy with
> `--dangerously-skip-permissions` so crew tool calls don't prompt.

{{BODY}}
